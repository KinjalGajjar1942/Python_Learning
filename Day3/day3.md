<!-- "r" → Read (default). Error if file doesn’t exist.

"w" → Write (overwrites file). Creates new if not exist.

"a" → Append (adds data at the end). Creates new if not exist.

"x" → Create new file. Error if file already exists.

"b" → Binary mode (e.g., images, videos).

"t" → Text mode (default).

"r+" → Read & Write. -->

